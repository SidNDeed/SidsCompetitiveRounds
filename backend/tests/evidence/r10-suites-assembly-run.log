== the assembler's own self-test, both directions on every rule ==
cwd       <repo>
command   python backend/tests/evidence/assemble-evidence.py --selftest
selftest: 33 rule checks, all as stated
selftest rc=0

== r10-suites.txt, re-derived from the stdout it names ==
cwd       <repo>
command   python backend/tests/evidence/assemble-evidence.py --check backend/tests/evidence/r10-suites.txt
selftest: 33 rule checks, all as stated
OK: backend/tests/evidence/r10-suites.txt -- 9 claim(s) all derived from r10-suite-run.log; the verbatim block is the named stdout, byte for byte
check rc=0

== r10-postgresql-controls.txt, re-derived from the stdout it names ==
cwd       <repo>
command   python backend/tests/evidence/assemble-evidence.py --check backend/tests/evidence/r10-postgresql-controls.txt
selftest: 33 rule checks, all as stated
OK: backend/tests/evidence/r10-postgresql-controls.txt -- 6 claim(s) all derived from r10-pg-controls.log; the verbatim block is the named stdout, byte for byte
check rc=0

== r10-mutation-controls.txt, re-derived from the stdout it names ==
cwd       <repo>
command   python backend/tests/evidence/assemble-evidence.py --check backend/tests/evidence/r10-mutation-controls.txt
selftest: 33 rule checks, all as stated
OK: backend/tests/evidence/r10-mutation-controls.txt -- 1 claim(s) all derived from r10-mutation-run.log r10-freeze-guard.log r10-citation-pin.log; the verbatim block is the named stdout, byte for byte
check rc=0

== the REFUSAL, executed rather than described ==
A narrative claiming a count its stdout does not print is refused, and
the refusal names the count. The pair is fabricated here so the
direction is proved on the RULE rather than on whatever this directory
happens to hold; the same two directions are asserted from the closing
test below, and the same refusal is driven against the real report by
the mutation control suites-report-carries-an-underived-number.

cwd       <repo>
command   python -c "import importlib.util as u; s=u.spec_from_file_location('a','backend/tests/evidence/assemble-evidence.py'); m=u.module_from_spec(s); s.loader.exec_module(m); print('derivable   ->', m.undrivable('the run hashed 9 paths', '9 paths'))"
derivable   -> []
clean rc=0

cwd       <repo>
command   python -c "import importlib.util as u; s=u.spec_from_file_location('a','backend/tests/evidence/assemble-evidence.py'); m=u.module_from_spec(s); s.loader.exec_module(m); print('undrivable  ->', m.undrivable('the run hashed 12 paths', '9 paths'))"
undrivable  -> [('count', '12 paths')]
dirty rc=0

== the closing evidence check, which RUNS the assembler ==
The assembler is not a step somebody remembers to take: the committed
check below runs its self-test and re-checks every assembled report of
the newest round, so a report that stopped matching its own stdout reds
in the suite rather than in a log nobody re-reads.
cwd       <repo>/backend
command   python -m pytest tests/test_ffa_game_number_anchor.py -q -p no:cacheprovider -k "committed_evidence or assembled_from_its_run"
..                                                                       [100%]
2 passed, 153 deselected in 12.53s
closing rc=0
